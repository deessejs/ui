---
name: user-profile
description: User's role, stack, language preference, and how they want design topics framed
metadata:
  type: user
---

Works with AI coding agents to build UI and is trying to stop them producing generic
("slop") output. Stack is **shadcn/ui + Tailwind CSS v4**. Communicates in **French**;
wants written artifacts (docs, notes) in **English**.

Already fluent in the modern shadcn model — knows that semantic tokens replaced raw
palette classes (`bg-destructive`, not `bg-red-500`) and corrected me when I framed
raw-color drift as the current problem. Do not explain shadcn/Tailwind basics; assume
working knowledge of tokens, `@theme`, and the CLI.

**How to apply:** pitch at practitioner level. Lead with what changed recently and
what is non-obvious, not with fundamentals. See [[feedback-concrete-over-theory]] for
the framing preference and [[project-design-learnings]] for what is being built.
