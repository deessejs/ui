// Re-exports the build-time generated sources.
// Run `npm run prebuild` (or `npm run build`) to regenerate after adding
// new components or blocks to the registry.

export { SOURCES } from "./sources.generated"