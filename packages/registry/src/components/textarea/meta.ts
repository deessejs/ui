import type { ComponentMeta } from "../../types.ts"

export const meta: ComponentMeta = {
  id: "textarea",
  name: "Textarea",
  description:
    "Multi-line text area with token-driven borders, focus ring, and aria-invalid styling.",
  category: "forms",
}
