import type { ComponentMeta } from "../../types.ts"

export const meta: ComponentMeta = {
  id: "icon-button",
  name: "Icon Button",
  description: "Square button sized for a single icon. Requires an aria-label.",
  category: "buttons",
  variants: ["sm", "md", "lg"],
}